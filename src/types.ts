export interface Customer {
    id: string;
    name: string;
    since: string;
    revenue: string;
}

export interface Product {
    id: string;
    description: string;
    category: string;
    price: string;
}